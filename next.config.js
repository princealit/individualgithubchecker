/** @type {import('next').NextConfig} */
const nextConfig = {
  // Environment variables are now handled through .env files
  // No need to specify them here unless using custom logic
}

module.exports = nextConfig 